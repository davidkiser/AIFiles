apiVersion: twingate.com/v1beta
kind: TwingateConnector
metadata:
  name: my-kubernetes-connector
  namespace: twingate
spec:
  name: my-kubernetes-connector
  imagePolicy:
    provider: dockerhub
    schedule: "0 0 * * *"
