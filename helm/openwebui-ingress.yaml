apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: open-webui
  namespace: ai
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`localhost`) || Host(`openwebui.localhost`)
      kind: Rule
      services:
        - name: open-webui
          port: 80

